spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?serverTimezone=UTC
    username: root
    password: '0214'

#设置mp配置
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tbl_
