server:
  port: 80
spring:
  activemq:
    broker-url: tcp://localhost:61616
  jms:
    template:
      default-destination: itheima
    #发布订阅模型
#    pub-sub-domain: true
#  jsm:
#    template:
#      default-des
